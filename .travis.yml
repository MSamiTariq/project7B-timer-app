language: nodejs
node_js:
  - "stable"

script: -npm install
  -npm test
  -npm build

deploy:
  provider: surge
  skip_cleanup: true
  domain: timer-app2020.surge.sh
  project: ./build

on:
  branch: master
